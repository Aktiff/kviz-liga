"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export default function Admin(){const[password,setPassword]=useState("");const[error,setError]=useState("");const router=useRouter();async function login(){setError("");const res=await fetch("/liga/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});if(res.ok){router.push("/liga");router.refresh();}else setError("Nesprávne heslo");}return<div className="mx-auto max-w-sm"><h1 className="mb-4 text-2xl font-bold">Admin</h1><div className="space-y-3 rounded-xl border border-[#555555] bg-[#3d3d3d] p-4"><input type="password" className="w-full rounded-lg bg-[#333333] border border-[#666666] p-2" placeholder="Heslo" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==="Enter"&&login()}/>{error&&<p className="text-red-400 text-sm">{error}</p>}<button onClick={login} className="w-full rounded-lg bg-[#ffbf0b] py-2 hover:bg-[#ffd040]">Prihlásiť</button></div></div>;}
